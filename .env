# Environment variables for the node-blog application
# Rename values or set them in your deployment environment

# Application port (optional)
PORT=3000

# MongoDB connection string (replace with your URI)
MONGO_URI=mongodb://localhost:27017/blogdb

# JWT secret used for signing/verifying tokens — set to a strong secret
JWT_SECRET=change_this_to_a_strong_secret

# Node environment
NODE_ENV=development

# Optional: directory where uploaded files are served from
UPLOAD_DIR=uploads
